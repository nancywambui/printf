#include "main.h"
/**
 * _strlen - Returns the length
 * @s: pointer
 * Return: cha
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
		;
	return (c);

}
/**
 * _strlenc - Strlen function
 * @s: char pointer
 * Return: cha
 */
int _strlenc(const char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
		;
	return (c);
}
